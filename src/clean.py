"""Data cleaning.

Per-dataset cleaning: type coercion, missing-value handling, target
normalization. Each dataset keeps its own cleaning logic in its own lane.
"""

# TODO: clean a single dataset and write the result to data/processed/
