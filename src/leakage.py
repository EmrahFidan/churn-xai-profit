"""Leakage detection.

Per-dataset checks for target leakage and look-ahead features before modeling.
Flags suspicious columns; never merges datasets.
"""

# TODO: detect and report potential leakage per dataset
