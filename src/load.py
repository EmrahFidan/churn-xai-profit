"""Dataset loading.

Loads a single dataset's raw CSV from data/raw/ based on the registry in
config.yaml. One dataset at a time — datasets are never merged.
"""

# TODO: load a dataset by key from data/raw/, return features + target
